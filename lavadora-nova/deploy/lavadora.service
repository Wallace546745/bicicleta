# systemd: mantém o servidor da loja sempre no ar e o sobe no boot.
#   sudo cp deploy/lavadora.service /etc/systemd/system/
#   sudo systemctl daemon-reload && sudo systemctl enable --now lavadora
[Unit]
Description=Loja Bicicleta Elétrica V9 Max (Nerva + TikTok)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=loja
Group=loja
WorkingDirectory=/var/www/bicicleta/lavadora-nova/nerva
# o server.js lê o nerva/.env sozinho (env.js) — nada de segredo aqui
Environment=NODE_ENV=production
Environment=HOST=127.0.0.1
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=3
# logs: journalctl -u lavadora -f
StandardOutput=journal
StandardError=journal
# endurecimento básico
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ReadWritePaths=/var/www/bicicleta/lavadora-nova/nerva/data /var/www/bicicleta/lavadora-nova/p /var/www/bicicleta/lavadora-nova/index.html

[Install]
WantedBy=multi-user.target
